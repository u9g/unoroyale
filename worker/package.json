{
  "name": "card-royale-match",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute uno-stats --remote --file schema.sql",
    "db:migrate:local": "wrangler d1 execute uno-stats --local --file schema.sql",
    "test": "node test/match.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "~5.9.3",
    "wrangler": "^4.127.1",
    "ws": "^8.18.0"
  }
}
